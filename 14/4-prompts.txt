All memory should be dynamic, and the code should be elegant, concise, and best-practice. Ensure there are no placeholders. Follow the instructions in proj.pdf. If the code is already correct, acknowledge it; otherwise, provide a solution. Let's discuss before writing any code

Check the current project files, and tell me what is missing, because these instructions may have already been taken care of.
Check the files again, and let me know where do we stand, what's missing, and how to proceed from here.
let's focus on:

Encoding and Memory Image: (done)
"Implement 15-bit word encoding for instructions and data. Correctly set A.R.E. bits (Absolute, Relocatable, External). Handle all addressing modes (Immediate, Direct, Index, Register). Ensure proper instruction encoding with opcode and operands. Organize memory image, separating code and data segments. Verify correct handling of 8 registers (r0-r7) in all addressing modes. Use Two's complement for negative numbers in immediate addressing and arithmetic operations. Ensure all encoded words are exactly 15 bits wide."

Symbol Table and Directives: (done)
"Enhance symbol table to distinguish between code labels, data labels, external symbols, and entry points. Implement .data directive for integer arrays and .string directive for null-terminated strings. Ensure correct memory allocation and value encoding for data directives. Verify symbol attributes are properly set and retrieved during assembly process. Enforce maximum label length of 31 characters."

Two-Pass Algorithm and Macro Expansion: (done)
"Implement macro expansion in preprocessing stage. Develop first pass to build symbol table, calculate addresses, and handle directives. Create second pass to resolve addresses, generate final machine code, and handle external/entry symbols. Ensure proper interaction between passes and accurate address resolution for all symbol types. Handle forward references correctly in the second pass."

Output Generation:
"Generate .ob file with correct memory image formatting, including accurate memory addresses in decimal and content in binary. Create .ent file listing all entry points and their addresses. Produce .ext file showing usage instances of external symbols with addresses. Verify correct implementation and encoding of all 16 instructions (mov, cmp, add, sub, lea, clr, not, inc, dec, jmp, bne, red, prn, jsr, rts, stop). Ensure output files accurately reflect assembled program structure and addressing."

-----
Error handling instructions:
1) Implement error reporting without halting execution. 2) Use a report_error(int line_number, const char* message) function to print errors to stdout. 3) Include source file line numbers in error messages. 4) Continue processing the entire input file after encountering errors. 5) Maintain a global error count. 6) Only generate output files (.ob, .ent, .ext) if error count is zero after full processing. 7) Do not use exit() or return prematurely due to errors.